/**
 * Barrel export for shared validation/utility helpers.
 *
 * Import from "@/lib/validations" — keeps import paths short and stable.
 *
 * @example
 * import { slugify, paginate, buildPaginationMeta } from "@/lib/validations";
 */
export * from "./slug";
export * from "./pagination";
